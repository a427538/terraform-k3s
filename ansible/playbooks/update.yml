- hosts: web
  become: yes
  tasks:
  - name: Update and upgrade apt packages
    apt:
      upgrade: yes
      update_cache: yes
      cache_valid_time: 86400
    register: apt_status
    until: apt_status is success
    delay: 6
    retries: 10 